# SQLite databases
*.db
*.db?*
*.db-journal
*.db-wal
*.db-shm

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock
last-touched
sync-state.json
.sync.lock

# Local state (not shared across clones)
.local_version
redirect
sync_base.jsonl

# Legacy database files
db.sqlite
bd.db

# Merge artifacts (temporary files from 3-way merge)
beads.base.jsonl
beads.base.meta.json
beads.left.jsonl
beads.left.meta.json
beads.right.jsonl
beads.right.meta.json

# JSONL data files (daemon manages these on beads-sync branch)
issues.jsonl
interactions.jsonl

# Keep metadata and config tracked (shared across clones)
!metadata.json
!config.json
